
  # MiProveedor – Tienda del Cliente (client store)

  This is a code bundle for MiProveedor – Tienda del Cliente (client store). The original project is available at https://www.figma.com/design/LgtdRw5GozIPgS36fbp4nt/MiProveedor-%E2%80%93-Tienda-del-Cliente--client-store-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  